#include <stdio.h>
#include <stdlib.h>

typedef struct no {
    int chave;
    int x;
    struct no *esq;
    struct no *dir;
} no;
typedef no *arvore;

int main() {
    int c, n;

    scanf("%d", &c);

    for (int i = 0; i < c; i++) {
        r = malloc(sizeof(no) * n);
        
        scanf("%d", &n);

        for (int j = 0; j < n; j++) {
            scanf("%d", &n);
        }
    }
}