// nao finalizado

#include <stdio.h>
#include <stdlib.h>

int main() {
    int n, m, c;

    scanf("%d", &n);

    for (int i = 0; i < n; i++) {
        scanf("%d %d", &m, &c);
        int v[m];

        for (int j = 0; j < c; j++) {

        }
    }
}